require('window-picker').setup({
    current_win_hl_color = '#BF616A',

    -- all the windows except the current window will be highlighted using this
    -- color
    other_win_hl_color = '#A3BE8C',
})
